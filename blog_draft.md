# 自作タイル型ウィンドウマネージャを作った

こんにちは、Toadです。

今回は、Mac 用のタイル型ウィンドウマネージャを作ってみたので、その紹介をしようと思います。

## なぜ作ったのか？

私は普段、キーボードから手を離さずに作業したいという強いこだわりがあります。
Mac には [Yabai](https://github.com/koekeishiya/yabai) や [AeroSpace](https://github.com/nikitabobko/AeroSpace) といった素晴らしいタイル型ウィンドウマネージャが存在します。
これらも試してみたのですが、より直感的に、マウスの操作をキーボードで実現するような、そんなタイル型ウィンドウマネージャが欲しいと思いました。

ここで紹介しているキーボードショートカットなどは、とりあえず私の手になじむように設定した一例です。
ゆくゆくは、自分でも簡単にホットキーを変更できるように、設定画面からカスタマイズする機能も実装する予定です。

## こだわりの機能

### 1. 自動タイリング機能

タイリング型のウィンドウマネージャなので、アプリを起動するだけで自動的に画面いっぱいにタイル状に配置されます。

もちろん、Finder や設定アプリなど「フローティング（重なり合った状態）のままにしたいアプリ」もあります。
そういったアプリは、設定で除外リストに追加しておけば、自動タイリングの対象から外すことができます。

また、現在フォーカスが当たっているウィンドウには枠線（ボーダー）が表示されるため、今どこを操作しているかが一目でわかります。


### 2. Vimのようなモード

最大の特徴は、Vim のような モード の概念を取り入れたことです。

一般的なウィンドウマネージャでは、すべての操作に Ctrl + Option + Shift + ... のような複雑なショートカットを割り充てがちです。
しかし、それでは指が吊ってしまいますし、覚えきれません。

そこで、操作したい内容に合わせてモードを切り替える設計にしました。

- Normal Mode: ウィンドウの移動やフォーカス切り替え
- Window Select Mode: ウィンドウの複数選択や並び替え
- Gap Select Mode: ウィンドウサイズの調整（隙間の移動）

このアプリでは、矢印キー（I, K, J, L）を基本として操作を行います。
モードを切り替えることで、同じ矢印キーでも、フォーカス移動、リサイズ、ウィンドウ移動 と役割を変化させることができます。
これにより、少ないキー数で多彩な操作を直感的に行えるようになりました。

以前の記事 Mac備え付けキーボードのキーマップを魔改造した でも紹介した通り、私は物理的なキー配置に基づいた逆 T 字型 (I, K, J, L) の操作が直感的で気に入っているため、これをデフォルトとして採用しました。

もちろん、Vim といえば H, J, K, L だろう という方も多いと思います。
現在はソースコードに直接このキーコードを埋め込んでいるため、変更するにはコードを書き換える必要がありますが、前述の通り、ゆくゆくは設定画面から自由に変更できるようにする予定です。

### 3. ウィンドウのフォーカスの移動

Ctrl + Option + J / K / L / I で、隣のウィンドウへフォーカスを移動できます。
モニター間の移動もシームレスに行えるため、マウスに手を伸ばす必要がありません。

### 4. ウィンドウ配置の変更

Ctrl + Option + Shift + J / K / L / I で、現在フォーカスしているウィンドウ自体を移動（スワップ）できます。
このウィンドウを右側に持っていきたい といった並び替えも、キーボードだけで一瞬です。

### 5. ウィンドウの整理 (Window Select Mode)

複数のウィンドウをまとめて操作したいときは、Ctrl + Option + S で Window Select Mode に入ります。
移動したいウィンドウを選んで（Enter）、まとめて移動させることができます。

さらに便利なのが、縦割り（スタック） の機能です。
基本のレイアウトは横並び（列）ですが、参考文献とエディタを縦に並べてセットで扱いたい という場面があると思います。

そんな時は、ウィンドウを複数選択して V を押します。
すると、選択したウィンドウが縦に結合（マージ）され、一つのスタックになります。
逆に、スタックされたウィンドウを選択して Shift + V を押せば、再び個別の列に分割（スプリット）されます。

(ここに Merge/Split の GIF を貼る)

### 6. ウィンドウのリサイズ
2通りのリサイズ方法を用意しています。

#### ノーマルモード
Ctrl + Option + - / + で、現在のウィンドウ幅を縮小・拡大できます。

#### Gap Select Mode (ギャップ選択モード)
これが今回一番作りたかった機能です。
ウィンドウの間の隙間（Gap）を選択して、それを動かす というアプローチです。

1. Ctrl + Option + G で Gap Select Mode に入る
2. 矢印キーで、動かしたい「隙間」を選択（隙間がハイライトされます）
3. Enter で確定し、そのまま矢印キーで隙間を移動させる

右を広げる ではなく この境界線を右にずらす という感覚で操作できるので、直感的にレイアウトを微調整できます。

(ここに Gap Select Mode の GIF を貼る)

### 6. その他の便利機能

#### Layout Reset (レイアウト一発リセット)
レイアウトが崩れてきたら、R キー一発で シンプルな縦列配置 にリセットできます。心の平穏を取り戻すための機能です。

(ここに Reset Layout の GIF を貼る)

#### Monitor Cursor / Virtual Desktop
- Monitor Cursor (M / Q): マウスカーソルを瞬時に別のモニターの中央に飛ばします。デュアルモニター環境で、マウスどこ行った？ からの脱却です。
- Virtual Desktop (U / O): 仮想デスクトップ（Space）の移動もホームポジションから。


## 最後に

というわけで、私が欲しかった機能を詰め込んだ、私のためだけのウィンドウマネージャを紹介しました。 
公開するようなプロダクトではありませんが、せっかく作ったのでコードは GitHub に上げておきます。
もし自分もこういうのが作りたい！ という方がいれば、参考程度に GitHub を覗いてみてください。


